import {useEffect} from "react";
import {axiosAuth} from '../utils/axiosInstance.js'



export const useCheckAuth = () => {

    // const [user, setUser] = useRecoilState(userAtom)
    //
    // useEffect(() => {
    //     axiosAuth( '/user' )
    //         .then( ({ data }) => setUser({ ...user, email: data.email }) )
    // }, [])

}

